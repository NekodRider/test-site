version: '3'

services:
  web:
    build: ./web
    restart: always
    command: gunicorn -w 5 -b :8080 run:app

  nginx:
    build: ./nginx
    restart: always
    ports:
      - "80:80"
    depends_on:
      - web
